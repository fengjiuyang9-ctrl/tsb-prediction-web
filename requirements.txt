--extra-index-url https://download.pytorch.org/whl/cpu
tornado>=6.3.0
# Linux (Railway): force CPU-only wheels to avoid pulling CUDA dependency packages
torch==2.2.2+cpu ; platform_system == "Linux"
torchvision==0.17.2+cpu ; platform_system == "Linux"
# Windows local dev fallback
torch==2.2.2 ; platform_system == "Windows"
torchvision==0.17.2 ; platform_system == "Windows"
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
Pillow>=10.0.0
